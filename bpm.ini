[package]
name=path
version=1.0.0
include=libon
description=Trigger actions based on events.

[dependencies]
is=*
log=*
string=*

[devDependencies]
embed-docs=*
unittest=*

[scripts]
readme=embed-docs README.md libon
test=unittest test/*.test
